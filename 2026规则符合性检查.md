# 2026 中国大学生方程式系列赛事规则符合性检查（E42 驱动控制）

> 依据：《2026中国大学生方程式系列赛事规则（答题版）》PDF
> 检查对象：E42 驱动控制模型（E42_优化\text28.slx，含 Drive_AEKF）

## 1. 模型相关条款符合性

| 条款 | 要求摘要 | 状态 | 说明 |
| --- | --- | --- | --- |
| T12.9.5 | 至少两个独立 APPS | 满足 | 角度1（ch52）、角度2（ch53），独立 ADC 通道 |
| T12.9.6 | 模拟传感器传递函数不同 | 硬件确认 | 两路标定不同（初始值/量程 960/1800 与 970/1780），实车需确认 |
| T12.9.8 | APPS 不可靠超过 100 ms 必须切断动力 | 满足（更严） | 不可靠立即清零；Drive_AEKF 输出门控兜底 |
| T12.9.9 | 两传感器差值 >10% 判不可靠 | 满足 | `不可靠信号判断`：u1>10（百分比差值） |
| T12.9.12 | 完全松开踏板 → 轮上扭矩 ≤ 0 | 本次修复 | `aekf_comp`：T_base≤0 时强制 T_opt=0 |
| T12.10.1/2 | SCS 单点故障必须进入安全状态 | 基本满足 + 本次补强 | APPS SCS→系统关键信号→安全回路；电机 CAN 丢失 500 ms 保护新增 |
| T12.10.5 | 数字信号最大允许延迟 ≤ 500 ms | 本次实现 | 电机转速/扭矩连续 50 个周期（500 ms）无变化 → 判失效 |
| T12.6.1/2 | BSPD 独立非编程电路，冲突断开安全回路 | 硬件 | 模型内为软件冗余（同时油门刹车锁存切扭），硬件 BSPD 仍需制作 |
| T12.6.7 | 必须安装制动压力传感器 | 满足 | 前油压 ch54（I_AN15_5V） |
| T12.6.9 | 传感器信号线可单独断开 | 硬件 | 检查接插件设计 |
| EV4.11.5 | 进入待驶必须踩制动 + 额外动作 | 满足 | `待驶激活`：制动>0 且 按钮>0（AND） |
| EV4.11.6 | 安全回路断开立即退出待驶 | 满足 | EN 受安全回路（SwitchInput ch44）门控 |
| EV4.12 | 待驶鸣笛 1～3 s、80～90 dB | 满足 | 计数 0～150（约 1.5 s），受安全回路门控 |
| EV2.2.4 | 禁止通过驱动装置反转车轮 | 满足 | 扭矩指令下限 0 |
| T7.1.13 | 再生制动约束 | 不适用 | 模型未实现再生制动 |
| T7.2.5 | 制动测试用急停关闭驱动系统 | 流程/硬件 | 急停开关为硬件项 |

## 2. 本次依据规则做的修改（Drive_AEKF/aekf_comp）

### 2.1 电机 CAN 失效保护（T12.10.2-d / T12.10.5）

电机转速、扭矩反馈是影响输出扭矩的系统关键信号。原模型 `Livecounter` 算出后直接接 Terminator，无超时保护；AEKF 在 CAN 断线时会用陈旧数据继续补偿。

新增逻辑：

```
若 w_rpm 与 Te_raw 连续 50 个周期（10 ms × 50 = 500 ms）不变：
    → 判定 CAN 失效
    → 冻结 AEKF 状态，禁用前馈与主动阻尼
    → 输出退化为纯油门直通（T_base）
```

### 2.2 松开踏板 / APPS 故障强制零扭矩（T12.9.12 / T12.9.8）

原 AEKF 前馈在油门松开时仍可能叠加正扰动扭矩。新增门控：

```
若 T_base ≤ 0（踏板松开或 APPS 不可靠导致请求为 0）：
    → T_opt = 0（轮上扭矩 ≤ 0）
```

## 3. 仍需硬件完成的项目（不在模型范围）

- BSPD 独立非编程电路（T12.6）
- 急停开关、惯性开关、制动超程开关（T12.4/T12.5/T7.3）
- TSAL 状态机（EV4.10）
- APPS 传感器独立可切断接插件（T12.9.11）
- 绝缘监测 IMD、AMS 等（EV5/EV8）

## 4. 验证

- MATLAB R2014a 加载：正常
- `SimulationCommand = update`：UPDATE_OK
- 原模型 `E:\自研BMS和软件资料\E42\E42\text28.slx` 未改动
