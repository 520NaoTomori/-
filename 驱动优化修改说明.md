# text28 驱动部分优化修改说明（AEKF 组合版）

## 1. 硬件条件

本车为**后轮驱动**，实际可用传感器只有：

- 油门踏板角度传感器（模型 `角度传感器` 模块）
- 电机转速（MCU 经 CAN 回传，模型 `电机测速` 模块）
- 电机扭矩（MCU 经 CAN 回传，同一报文内）

**没有轮速传感器**，因此此前基于“前轮车速/滑转率”的 Drive_TCS 方案已移除，改为基于论文方法的 AEKF 组合方案。

## 2. 修改范围

- **只修改**：`text28/驱动系统/Subsystem` 内的驱动控制链路。
- **未修改**：上电流程、任务调度（10ms/20ms/50ms）、CAN 收发、RapidECU 配置、BMS/MCU 报文处理、待驶/制动逻辑、角度传感器处理、ECU 信号打包等基础框架。
- 原模型 `E:\自研BMS和软件资料\E42\E42\text28.slx` 保持原样未动。

## 3. 新方案：Drive_AEKF

在 `驱动系统/Subsystem` 中新增子系统 **`Drive_AEKF`**，串接在：

```
appsrule（油门扭矩请求） → Drive_AEKF → 油门输出 → ECU → MCU_can总线
```

同时接入两路已有 CAN 反馈：

- `电机测速/电机转速`（rpm）
- `电机测速/电机扭矩`（CAN 原始值）

### 3.1 算法来源

来自论文《基于自适应扩展卡尔曼滤波的FSEC赛车永磁同步电机转矩脉动抑制与主动阻尼控制策略研究》及配套仿真代码 `sim_3_6.m`。原论文为 4 状态电流域 AEKF（状态 `[id, iq, ω, Td]`，量测 `[id, iq]`）；本方案按 VCU 10 ms 任务与 CAN 反馈条件，降阶为**机械域 2 状态 AEKF**：

```
状态   x = [ω_hat; Td_hat]
量测   y = ω（电机转速，rpm 转 rad/s）
输入   u = Te（电机扭矩反馈，CAN 原始值按 0.1 N·m/LSB 换算）
模型   dω/dt = (Te - Td - B·ω) / J
```

保留论文的全部关键机制：

1. **NIS 门控遗忘因子**：新息归一化平方滑动平均（窗口 10），超过卡方阈值后在线降低遗忘因子 λ（0.85～0.98），加快突变跟踪；稳态自动回到 λ_max。
2. **协方差上界保护**：P 对角元按 `[100, 400]` 截断。
3. **Joseph 形式协方差更新**：保证数值正定性。
4. **故障安全**：NIS 持续越限 20 ms → 前馈补偿淡出（目标 0）；恢复正常 50 ms 后重新投入；淡出时间常数 10 ms。
5. **前馈补偿**：Td_hat 经 5 Hz 低通（10 ms 任务下替代原 250 Hz）、±6 N·m 限幅后叠加到扭矩指令。
6. **主动阻尼**：扭矩指令减去 `Kd·ω`（Kd = 0.01 N·m·s/rad），抑制转速振荡。
7. **输出斜率限制**：最终扭矩指令上升 ≤ 2500 原始单位/s、下降 ≤ 5000 原始单位/s，抑制阶跃冲击。

### 3.2 参数表（固化在 `aekf_comp` 函数内，可改）

| 参数 | 值 | 含义 |
| --- | --- | --- |
| Ts | 0.01 s | 控制周期（已确认驱动任务挂在 10ms1） |
| J | 0.003 kg·m² | 电机+传动折算转动惯量 |
| B | 5e-4 N·m·s/rad | 粘性阻尼 |
| Q1 / Q2 | 1e-3 / 1.0 | ω、Td 过程噪声方差 |
| R | 0.01 (rad/s)² | 转速量测噪声方差 |
| λmax / λmin | 0.98 / 0.85 | 遗忘因子上下界 |
| γ（gam） | 3.841 | 卡方阈值（自由度 1，95%，对应单转速量测） |
| a（aa） | 0.5 | sigmoid 切换系数 |
| Pcap | [100, 400] | 协方差对角元上限 |
| Kd | 0.01 | 主动阻尼系数 |
| WFF | 2π×5 rad/s | 前馈低通（10 ms 任务下约 5 Hz） |
| FFMAX | ±6 N·m | 前馈限幅 |
| RAW | 10 原始单位/N·m | 扭矩比例（0.1 N·m/LSB） |
| 输出饱和 | 0～5000 | 驱动扭矩下限 0、上限 5000 原始单位 |
| 斜率限制 | +25 / −50（每 10 ms 步） | 上升 2500/s、下降 5000/s |

## 4. 单位与协议核对（重要）

1. **扭矩比例 RAW=10**（CAN 原始值 ×0.1 = N·m）是依据模型内 `Demand_Torquee / MotorTorque` 饱和 ±5000 与论文电机峰值 165 N·m 推断的默认值。**上线前必须按实际 MCU 报文缩放核对**；若为 1 N·m/LSB，把 RAW 改为 1。
2. **必须确认 MCU 报文确实包含电机扭矩回传**（模型 `电机测速/ReceiveMessages` 已有 MotorTorque 解包）。若只有转速，AEKF 将无法估计 Td，需要改用电流回传或命令扭矩。
3. 驱动任务已确认为 **10ms1**，与函数内 Ts=0.01 一致；若改任务周期，须同步修改 Ts。

## 5. 验证结果

- MATLAB R2014a（8.3.0.220）加载优化后模型：正常。
- `SimulationCommand = update`：**UPDATE_OK**，无结构错误。
- Drive_AEKF 内部：12 个模块、15 条连线；子系统级连线：`appsrule → Drive_AEKF`、`Drive_AEKF → 油门输出`、`电机测速（转速/扭矩）→ Drive_AEKF`。

## 6. 使用注意事项

1. 嵌入式模型修改后**必须重新生成代码**；`text28_ec55xx_rtw` 与 `驱动系统工程` 中固件仍对应旧模型，切勿直接烧录。
2. 实车标定顺序建议：先确认 MCU 扭矩缩放（RAW）→ 台架验证 AEKF 收敛 → 再调 Kd、FFMAX、斜率。
3. 起步阶段若仍出现瞬时负向冲击，优先加大 Kd 或降低 FFMAX；若感觉动力响应偏肉，可调高输出斜率上限。
4. 若后续加装轮速传感器，可把论文后续方向（牵引力控制）再叠加回来；当前方案无此依赖。

## 7. 2026 赛事规则合规更新

依据《2026中国大学生方程式系列赛事规则（答题版）》对驱动控制部分做了两条补充（均在 Drive_AEKF/aekf_comp 内，未动框架）：

1. **电机 CAN 失效保护（T12.10.2-d / T12.10.5）**：电机转速/扭矩反馈连续 50 个周期（500 ms）不变即判 CAN 失效，冻结 AEKF、禁用前馈与阻尼，输出退化为纯油门直通。
2. **松开踏板强制零扭矩（T12.9.12 / T12.9.8）**：T_base≤0 时输出强制为 0，防止前馈叠加正扭矩。

详细符合性检查见《2026规则符合性检查.md》。

## 8. 驱动部分清理（2026-08-11）

对驱动逻辑做了整体体检并清理，**未改动任何控制逻辑与上电/框架**：

### 8.1 删除的死代码

- `前轮转速`（ch6/7 轮速采集）——实车无轮速传感器，且输出从未使用
- `滑转率计算`（滑转率公式）——算出来只接 Terminator
- `驱动逻辑` 中 `车速`、`滑转率`、`电机转速`、`电机扭矩` 四个输出口及对应 Terminator
- `电机测速` 内的 `转速转轮速`、`后轮速度` 换算链（唯一下游就是已删除的滑转率计算）

### 8.2 重命名（消除混乱）

| 原名 | 新名 |
| --- | --- |
| Subsystem（驱动逻辑容器） | 驱动逻辑 |
| Subsystem2 | 安全回路输出 |
| appsrule | 油门规则 |
| 传递函数（输出口） | 油门请求 |
| 油门1 / 油门 / 刹车 | 油门原始值 / 油门百分比 / 刹车有效 |
| 同时油门刹车 / 无刹车判断 / 油门小于5%判断 / 动力信号判断 | 油门刹车冲突 / 刹车判断 / 油门死区判断 / 动力允许判断 |
| 油门数值（子系统）/ 百分比转化 | 油门数值处理 / 百分比转化 |
| 制动与待驶（含换行符）、油门输出、使能、油压1/油压2 等 | 去掉换行符/整理命名 |
| ECU 扭矩输入 / Res / Enble / Motor / MCU_can总线 | 扭矩请求 / 保留 / 使能 / 电机报文打包 / MCU_CAN发送 |

### 8.3 验证

- 死块全部删除，`驱动逻辑` 输出口缩减为 4 个：`油门输出`、`复位`、`使能`、`系统关键信号`
- `电机测速` 输出口缩减为 2 个：`电机转速`、`电机扭矩`
- Drive_AEKF 接线完整：`T_base / w_rpm / Te_raw → T_opt`
- MATLAB R2014a 加载正常，`Simulink update` 通过
